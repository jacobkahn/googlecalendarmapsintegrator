
calendarList = {"dictionary returned by basic calendar call to get email address"}



function parse_calendar_name(calendarList)
{
	if (calendarList.kind === "calendar#calendarList")
	{
	for (i = 0; i < json.length; i++)
	{
		calendarList."items".
	}
	return json_full
	}
	
}



function parse_full_JSON_object(json_full){
	for (i = 0; i < json.length; i++) 
	{   
		var event_name = json.items[i].summary
		var start_time = json.items[i].start.datetime
		var end_tie = json.item[i].end.datetime
	}
}